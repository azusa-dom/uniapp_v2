# Assets 资源目录

用于存放用于网页展示的截图、视频、品牌元素等静态文件。

- 将截图命名为 `screenshot-home.png`、`screenshot-parent.png` 等，方便引用。
- 如果放置视频，可考虑 `.webm` 或 `.mp4`；静态站点会直接引用。
- 部署到 GitHub Pages / Vercel 时，该目录会原样发布。

> 如暂时没有素材，可以保留此占位文件，避免 Git 忽略空目录。

